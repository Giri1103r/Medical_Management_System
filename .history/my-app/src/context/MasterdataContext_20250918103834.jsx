import React, { createContext, use } from 'react'
import axios from "axios";

export MasterdataContext = createContext();

const MasterdataContext = () => {
  return (
    <div>MasterdataContext</div>
  )
}

export default MasterdataContext